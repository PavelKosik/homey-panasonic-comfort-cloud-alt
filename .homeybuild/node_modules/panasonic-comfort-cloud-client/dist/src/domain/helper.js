"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.padTo2Digits = padTo2Digits;
exports.getDateForHistoryData = getDateForHistoryData;
exports.getTimezoneForHistoryData = getTimezoneForHistoryData;
exports.getFormattedTimestamp = getFormattedTimestamp;
function padTo2Digits(num) {
    return num.toString().padStart(2, '0');
}
function getDateForHistoryData(date) {
    return ([
        date.getFullYear(),
        padTo2Digits(date.getMonth() + 1),
        padTo2Digits(date.getDate()),
    ].join(''));
}
function getTimezoneForHistoryData(date) {
    var tzo = -date.getTimezoneOffset();
    var dif = tzo >= 0 ? '+' : '-';
    const pad = function (num) {
        return (num < 10 ? '0' : '') + num;
    };
    return dif + pad(Math.floor(Math.abs(tzo) / 60));
}
function getFormattedTimestamp() {
    const now = new Date();
    const year = now.getFullYear();
    const month = String(now.getMonth() + 1).padStart(2, '0'); // Months are 0-based
    const day = String(now.getDate()).padStart(2, '0');
    const hours = String(now.getHours()).padStart(2, '0');
    const minutes = String(now.getMinutes()).padStart(2, '0');
    const seconds = String(now.getSeconds()).padStart(2, '0');
    return `${year}-${month}-${day} ${hours}:${minutes}:${seconds}`;
}
