export declare class CFCGenerator {
    private FIXED_KEY;
    private COMFORT_CLOUD;
    toUTF8String(str: string): Buffer;
    processTimestamp(dateString: string): number;
    constructBuffer(timestamp: string, token?: string | null): Buffer;
    insertCfc(hash: string): string;
    generateKey(timestamp?: string | null, token?: string | null): string;
}
