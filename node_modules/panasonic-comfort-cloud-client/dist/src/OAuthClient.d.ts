import { AxiosResponse } from "axios";
export declare class OAuthClient {
    private oauthClient;
    private appVersion;
    private enableAutoRefresh;
    private clientId;
    token: string;
    tokenRefresh: string;
    private readonly BASE_URL;
    private readonly CLIENT_ID;
    private readonly OAUTH_CLIENT_ID;
    private readonly REDIRECT_URL;
    constructor(appVersion?: string, enableAutoRefresh?: boolean);
    oAuthProcess(username: string, password: string): Promise<string>;
    private authorize;
    private authorizeRedirect;
    private login;
    private loginCallback;
    private getNewToken;
    refreshToken(tokenRefresh?: string): Promise<string | null>;
    private loginRedirect;
    generateRandomString(length: number): string;
    getQuerystringParameterFromHeaderEntryUrl(response: AxiosResponse, headerEntry: string, querystringParameter: string, baseUrl: string): string | null;
}
